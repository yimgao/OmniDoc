# =============================================================================
# OmniDoc Frontend - Development Environment Configuration
# =============================================================================
# Next.js automatically loads .env.development in development mode
# Or copy to .env.local for manual loading

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Backend API URL (development - local backend)
NEXT_PUBLIC_API_BASE=http://localhost:8000

# WebSocket URL (auto-generated from API_BASE, but can be overridden)
# NEXT_PUBLIC_WS_URL=ws://localhost:8000

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
# Next.js automatically sets NODE_ENV=development in dev mode
# NODE_ENV=development
