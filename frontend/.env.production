# =============================================================================
# OmniDoc Frontend - Production Environment Configuration
# =============================================================================
# Next.js automatically loads .env.production in production mode
# For Vercel deployment, set these in Vercel dashboard: Settings → Environment Variables

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Backend API URL (production - Oracle Cloud)
NEXT_PUBLIC_API_BASE=https://api.omnidoc.info

# WebSocket URL (auto-generated from API_BASE, but can be overridden)
# NEXT_PUBLIC_WS_URL=wss://api.omnidoc.info

# -----------------------------------------------------------------------------
# Environment
# -----------------------------------------------------------------------------
# Next.js automatically sets NODE_ENV=production in production mode
# NODE_ENV=production
